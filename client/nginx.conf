server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA: URL nào không khớp file tĩnh thì trả về index.html
        # để Vue tự xử lý, tránh lỗi 404 khi F5 ở route con.
        try_files $uri $uri/ /index.html;
    }
}
